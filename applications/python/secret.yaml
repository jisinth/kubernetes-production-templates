# secret.yaml — PLACEHOLDER ONLY. Do not put real credentials here.
#
# THIRD_PARTY_API_KEY and ENCRYPTION_KEY both decode to "changeme".
#
# In a real cluster:
#   - Seal this file with kubeseal before committing, see
#     ../../manifests/sealed-secrets, or
#   - Use external-secrets.io to sync these values from a real secrets
#     manager (Vault, AWS Secrets Manager, GCP Secret Manager) at deploy
#     time instead of storing secret material in git.
apiVersion: v1
kind: Secret
metadata:
  name: python-app-secret
  namespace: default
  labels:
    app.kubernetes.io/name: python-app
type: Opaque
data:
  THIRD_PARTY_API_KEY: Y2hhbmdlbWU=
  ENCRYPTION_KEY: Y2hhbmdlbWU=
