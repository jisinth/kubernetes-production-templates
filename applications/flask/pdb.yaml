apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: flask-app
  namespace: default
  labels:
    app.kubernetes.io/name: flask-app
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: flask-app
