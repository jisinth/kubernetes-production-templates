apiVersion: v1
kind: Service
metadata:
  name: springboot-app
  namespace: default
  labels:
    app.kubernetes.io/name: springboot-app
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: springboot-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
